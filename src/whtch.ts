import {
  action,
  makeAutoObservable,
  makeObservable,
  observable,
  computed,
} from "mobx";
import { observer } from "mobx-react";

export {
  action,
  makeAutoObservable,
  makeObservable,
  observable,
  observer,
  computed,
};
